using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using System.Threading.Tasks;

namespace Methods_Assignment
{
    class Program
    {
        static void Main(string[] args)
        {
           
            Console.WriteLine("Enter Your Name:");
            string userName = Console.ReadLine();
            Console.WriteLine("Enter Your Age:");
            int userAge = Convert.ToInt32(Console.ReadLine());
            User user = new User(userName, userAge)
        }
    }//run the drivers licence method
    //run voting method
    // run drinking and tabacco method
    //run president method
    //run seenior citizen method

    // all methods to return if the user is old enough (age validation to receive benefit ) AND
    //also return the integer value of how many years until they do reach the right age 
}   // that's the plan anyways...































